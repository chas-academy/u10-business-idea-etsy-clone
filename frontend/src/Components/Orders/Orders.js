const Orders = () => {
  return <h1>This is the orders view</h1>;
};

export default Orders;

// todo:
// add logic in api
// add logic in state in app.js
// display what we get from api in component
// crud?
